export EDITOR='vim'

alias ll='ls -lh'
alias lla='ls -alh'
alias l='ls'
alias la='ls -a'
alias vi='vim'
alias qq='bwrap --dev-bind / / --tmpfs /usr/share/fonts/windows-zh_cn /opt/apps/com.qq.im.deepin/files/run.sh'
alias tim='bwrap --dev-bind / / --tmpfs /usr/share/fonts/windows-zh_cn /opt/apps/com.qq.office.deepin/files/run.sh'
alias true-rm='/bin/rm'
alias rm='trash-put'
alias pc='proxychains'

source /usr/share/nvm/init-nvm.sh

# yarn 
#export PATH=$HOME/.yarn/bin:$PATH

# custom bin
#export PATH=$HOME/.local/bin:$PATH
